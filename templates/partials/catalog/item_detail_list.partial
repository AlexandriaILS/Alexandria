{% load i18n %}

<div class="row mt-3">
    <div class="col-md-2">
        <div class="row">
            <div class="col">{% include 'partials/cover_image.partial' with item=el img_height=120 %}</div>
        </div>
    </div>
    <div class="col-md-1 text-center">
        <div class="col mt-3">
            {% include 'partials/catalog/item_detail_partials/itemtype_icon.partial' %}
        </div>
    </div>
    <div class="col-md-6 mt-4">
        <div class="row">
            <div class="col d-flex align-items-center">
                {% if checkout_view or hold_view %}
                    {% include 'partials/catalog/item_detail_partials/title.partial' %}
                {% else %}
                    {% include 'partials/catalog/item_detail_partials/publisher_info.partial' %}
                {% endif %}
            </div>
            <div class="col d-flex align-items-center">
                {% if checkout_view %}
                    {% include 'partials/catalog/item_detail_partials/due_date.partial' %}
                {% elif hold_view %}
                    {% include 'partials/catalog/item_detail_partials/hold_status.partial' %}
                {% else %}
                    {% include 'partials/catalog/item_detail_partials/checkout_status.partial' %}
                {% endif %}
            </div>
        </div>
        <div class="row mt-3">
            <div class="col d-flex align-items-center">
                {% include 'partials/catalog/item_detail_partials/call_number.partial' %}
            </div>
            <div class="col d-flex align-items-center">
                {% include 'partials/catalog/item_detail_partials/barcode.partial' %}
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="row mt-3">
            <div class="col-2 col-md-0 col-lg-2"></div>
            <div class="col-8 col-md-12 col-lg-8 text-center mt-2" style="height:100%">
                <div class="d-grid gap-2">
                    {% if checkout_view %}
                        {% include 'partials/catalog/item_detail_partials/renew_hold_button.partial' %}
                    {% elif hold_view %}
                        {% include 'partials/catalog/item_detail_partials/cancel_hold_button.partial' %}
                    {% else %}
                        {% include 'partials/catalog/item_detail_partials/create_hold_button.partial' %}
                    {% endif %}
                </div>
            </div>
            <div class="col-2 col-md-0 col-lg-2"></div>
        </div>
    </div>
</div>
